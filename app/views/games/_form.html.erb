<%= form_with(model: game, local: true) do |form| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
  <div class="col s6">
    <select class="browser-default" name="game[score][east][player_id]" required>
      <option value="" disabled selected>Восток</option>
      <% @players.each do |player| %>
      <option value="<%= player.id %>"><%= player.name %></option>
      <% end %>
    </select>
  </div>
  <div class=" col s6">
    <input name="game[score][east][player_score]" id="east_player_score" type="number" class="validate" onchange="result_sum();" required />
  </div>
</div>

<div class="row">
  <div class="col s6">
    <select class="browser-default" name="game[score][south][player_id]" required>
      <option value="" disabled selected>Юг</option>
      <% @players.each do |player| %>
      <option value="<%= player.id %>"><%= player.name %></option>
      <% end %>
    </select>
  </div>
  <div class="col s6">
    <input name="game[score][south][player_score]" id="south_player_score" type="number" class="validate" onchange="result_sum();" required />
  </div> 
</div>

<div class="row">
  <div class="col s6">
    <select class="browser-default" name="game[score][west][player_id]" required>
      <option value="" disabled selected>Запад</option>
      <% @players.each do |player| %>
      <option value="<%= player.id %>"><%= player.name %></option>
      <% end %>
    </select>
  </div>
  <div class="col s6">
    <input name="game[score][west][player_score]" id="west_player_score" type="number" class="validate" onchange="result_sum();" required />
  </div>  
</div>
<div class="row">
  <div class="col s6">
    <select class="browser-default" name="game[score][north][player_id]" required>
      <option value="" disabled selected>Север</option>
      <% @players.each do |player| %>
      <option value="<%= player.id %>"><%= player.name %></option>
      <% end %>
    </select>
  </div>
  <div class="col s6">
    <input name="game[score][north][player_score]" id="north_player_score" type="number" class="validate" onchange="result_sum();" required />
  </div>  
</div>
<div class="row">
  <div class="actions col s6">
    <%= form.submit "Продолжить", :class => "btn waves-effect waves-light" %>
  </div>
  <div class="col s6" id="result_SUM" style="font-size: 22pt;">
  </div>
</div>

<% end %>