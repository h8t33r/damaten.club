<%= form_with(model: game, local: true) do |form| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
  <div class="col s6">
    <select class="browser-default" name="game[score][east][player_id]">
      <option value="" disabled selected>Восток</option>
      <% @players.each do |player| %>
      <option value="<%= player.id %>"><%= player.name %></option>
      <% end %>
    </select>
  </div>
  <div class=" col s6">
    <input name="game[score][east][score]" id="east_player_score" type="number" class="validate">
  </div>
</div>

<div class="row">
  <div class="col s6">
    <select class="browser-default" name="game[score][south][player_id]">
      <option value="" disabled selected>Юг</option>
      <% @players.each do |player| %>
      <option value="<%= player.id %>"><%= player.name %></option>
      <% end %>
    </select>
  </div>
  <div class="col s6">
    <input name="game[score][south][score]" id="sauce_player_score" type="number" class="validate">
  </div> 
</div>

<div class="row">
  <div class="col s6">
    <select class="browser-default" name="game[score][west][player_id]">
      <option value="" disabled selected>Запад</option>
      <% @players.each do |player| %>
      <option value="<%= player.id %>"><%= player.name %></option>
      <% end %>
    </select>
  </div>
  <div class="col s6">
    <input name="game[score][west][score]" id="west_player_score" type="number" class="validate">
  </div>  
</div>
<div class="row">
  <div class="col s6">
    <select class="browser-default" name="game[score][north][player_id]">
      <option value="" disabled selected>Север</option>
      <% @players.each do |player| %>
      <option value="<%= player.id %>"><%= player.name %></option>
      <% end %>
    </select>
  </div>
  <div class="col s6">
    <input name="game[score][north][score]" id="north_player_score" type="number" class="validate">
  </div>  
</div>

<div class="actions" style="padding-top: 20px;">
  <%= form.submit "Продолжить", :class => "btn waves-effect waves-light" %>
</div>

<% end %>